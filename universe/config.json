{
  "type": "object",
  "description": "DC/OS UI as a service.",
  "properties": {
    "service": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "title": "DC/OS UI service name",
          "description": "The name of the DC/OS UI service instance.",
          "default": "dcos-ui"
        },
        "marathon": {
          "type": "string",
          "title": "Marathon on Marathon service name",
          "description": "The Marathon-on-Marathon service name the UI instance should connect to.",
          "default": "marathon-user"
        }
      },
      "required": [
        "dcos-ui",
        "marathon"
      ]
    }
  }
}