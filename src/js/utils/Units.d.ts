export default class Units {
  static formatResource(resource: any, value: any): any;
  static filesize(
    size: any,
    decimals: any,
    threshold?: any,
    multiplier?: any,
    units?: any
  ): string;
}
