import * as React from "react";

const ConfigurationMapSection: React.FunctionComponent<{}> = props => {
  return <div className="configuration-map-section">{props.children}</div>;
};

export default ConfigurationMapSection;
